[Unit]
Description=NodeJs app for devops lessons

[Service]
User=www-data
WorkingDirectory={{project_root}}/node-app
Environment=PORT={{node_port}}
ExecStart=/usr/bin/node index.js
Restart=always

[Install]
WantedBy=multi-user.target
